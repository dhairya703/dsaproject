import 'package:flutter/material.dart';
class Lawyerplat extends StatelessWidget {
  const Lawyerplat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
