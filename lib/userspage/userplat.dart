import 'package:flutter/material.dart';
class Userplat extends StatelessWidget {
  const Userplat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
